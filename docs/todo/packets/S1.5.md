# Slice Packet: S1.5

## Goal

Add small pure-logic tests around compliance status mapping, actor normalization, and projection selection logic.

## Why Now

The current automated safety net is still integration-heavy. This slice should add a faster, narrower test layer for pure logic.

## Read These Files Only First

- `readme.md`
- `docs/todo/execution-board.md`
- `docs/archive/analysis/verification-gaps-and-follow-up.md`
- `Sentinel.Api/features/compliance/ComplianceServices.cs`
- `Sentinel.Api.Tests/SentinelApiFactory.cs`
- `Sentinel.Api.Tests/ComplianceApiTests.cs`

## Allowed To Edit

- `Sentinel.Api.Tests/*`
- `Sentinel.Api/Properties/AssemblyInfo.cs`
- `Sentinel.Api/Sentinel.Api.csproj`

## Do Not Edit

- `Sentinel.Api.Tests/ComplianceApiTests.cs`
- `diagnostics/*`
- `docs/*`
- `Sentinel.Shared/*`

## Interfaces Affected

Only internal testability seams are allowed.

## Testability Rule

Prefer the smallest internals-friendly seam over production refactoring.

Recommended approach:

- expose internals to `Sentinel.Api.Tests` via `AssemblyInfo.cs` if needed
- add one new dedicated unit-test file under `Sentinel.Api.Tests`
- only edit `ComplianceServices.cs` if a tiny pure helper extraction is truly necessary and does not change behavior

## Required Test Areas

Add lightweight tests for:

1. status mapping to `accepted | processed | failed`
2. actor-header normalization, including trimming and max-length clamping
3. earliest accepted-time selection semantics
4. terminal-event selection semantics where multiple ledger events exist

## Implementation Steps

1. Add a dedicated pure-logic test file instead of extending the integration-heavy file.
2. Introduce the smallest possible internal visibility/testability seam.
3. Keep tests independent of `WebApplicationFactory` and containerized infrastructure where possible.

## Acceptance Criteria

- The new tests exercise pure logic with little or no app bootstrapping.
- Existing behavior is unchanged.
- `ComplianceApiTests.cs` is left untouched in this slice.

## Verification

Run:

- `dotnet test Sentinel.Api.Tests/Sentinel.Api.Tests.csproj`
- `dotnet build Sentinel.sln`

## Out Of Scope

- new API endpoint tests
- diagnostics work
- Stage 4 harness work

## Return Format

Return:

- summary of logic covered
- files touched
- exact test command run
- whether internals exposure or helper extraction was required
- recommended next slice

## Next Slice On Success

`S1.6`
